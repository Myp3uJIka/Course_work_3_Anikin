1. В предыдущем ДЗ, когда мы хешировали пароль пользователя, использовалась функция base64.b64encode(). В данном
задании говорится о том, что необходимо сохранять хэш. Правильно ли я понимаю, что кодировать необязательно?

2. Каким образом следовало проводить аутентификацию? Правильно ли реализована проверка пользователя с помощью
декодирования токена?

3. Какой смысл скрывают строки во вью:
@genres_ns.response(200, "OK")
@genres_ns.response(404, "Genre not found")
? Т.е. реакцию после выполнения кода реализует return, зачем таким образом использовать response и как он работает?

4. Прошу разъяснить в каком виде докер-проект ожидает данные на страницах:
    - movies/id
    - genre/id
    - directors/id
    - favorites
? Мне не удалось выяснить это самостоятельно. Также прошу подсказать, как я мог получить эту информацию самостоятельно
(т.е. какие инструменты мне могли помочь в этом вопросе)?

5. Прошу подсказать каким образом должна проходить идентификация пользователя при запросе на GET /favorites/movies/
если при переходе по ссылке из докер-проекта не передаётся токен в request?

6. Если есть такая возможность, могу ли я получить пример описания Readme. Пока не представляю какую информацию по
проекту следует отразить в документации.

7. При написании тестов для DAO возникли сложности с db (fixture 'db' not found). Прошу подсказать как решается эта
проблема.